<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.agri.mapper.PermsMapper">
    <select id="getPermsOfRole" resultType="java.util.Map">
        select  a.path path, c.`role_key` name
        from sys_perm a LEFT JOIN sys_role_perm b on a.id = b.perm_id
        LEFT JOIN sys_role c on b.role_id = c.id
        where c.`status` = 0 and c.del_flag = 0 and a.`status`= 0 and a.del_flag = 0;
    </select>
</mapper>